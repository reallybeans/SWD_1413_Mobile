import 'package:flutter/material.dart';
class NavSettingView extends StatelessWidget {
  const NavSettingView({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Setting view'),
    );
  }
}