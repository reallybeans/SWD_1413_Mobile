import 'package:flutter/material.dart';
class NavSettingView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Setting view'),
      ),
    );
  }
}