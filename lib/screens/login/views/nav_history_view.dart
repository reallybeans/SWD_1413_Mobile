import 'package:flutter/material.dart';
class NavHistoryView extends StatelessWidget {
  const NavHistoryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('History view'),
      ),
    );
  }
}