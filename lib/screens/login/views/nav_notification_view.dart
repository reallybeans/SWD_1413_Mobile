import 'package:flutter/material.dart';
class NavNotifyView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Notification view'),
      ),
    );
  }
}