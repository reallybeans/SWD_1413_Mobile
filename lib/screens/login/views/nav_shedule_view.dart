import 'package:flutter/material.dart';
class NavSheduleView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('NavShedule view'),
      ),
    );
  }
}